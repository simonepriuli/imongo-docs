# Installation

To use IMongo you need npm installed on your machine.

```bash filename="Installation" copy
npm i imongo@latest
```

Note: to use some features of IMongo (schema validation) you also need to install Zod in your project.

```bash filename="Zod Installation" copy
npm i zod@latest
```
